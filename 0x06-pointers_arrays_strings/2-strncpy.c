#include "main.h"
/**
 * _strncpy - copys a string
 * @dest: parameter
 * @src: parameter
 * @n: parameter
 * Return: dest
 *
 */
char *_strncpy(char *dest, char *src, int n)
{
	for (i = o ; i < n : i++)
		dest[i] = src[i];
	return (dest);
}
